classDiagram
    class Пользователь {
        +Логин : string
        +ФИО : string
        +Должность : string
        +Роль : Role
        +проверить_пароль(пароль)
        +получить_полное_имя()
        +сменить_аватар(путь)
    }

    class Проект {
        +Название : string
        +Описание : text
        +Статус : ProjectStatus
        +Плановый_бюджет : float
        +рассчитать_процент_выполнения()
        +обновить_статус(новый_статус)
        +получить_список_задач()
        +рассчитать_рентабельность()
    }

    class Задача {
        +Название : string
        +Срок : date
        +Стоимость : float
        +Статус : TaskStatus
        +отметить_как_выполненную()
        +назначить_исполнителя(пользователь)
        +добавить_комментарий(текст)
        +записать_время(часы)
    }

    class Заявка_Лид {
        +Название : string
        +Бюджет : float
        +Статус : string
        +конвертировать_в_проект()
        +отклонить_заявку()
    }

    class Документ {
        +Название : string
        +Файл : file
        +Тип : DocumentType
        +скачать()
        +переименовать(новое_имя)
    }

    class ЭтапПроекта {
        +Название : string
        +Срок_начала : date
        +Срок_конца : date
        +получить_задачи_этапа()
    }

    class ЛогРаботы {
        +Часы : float
        +Дата : date
        +зафиксировать_время()
    }

    class Комментарий {
        +Текст : text
        +Дата : datetime
        +редактировать(новый_текст)
    }

    class СообщениеЧата {
        +Текст : text
        +Правки : bool
        +отправить()
    }

    class Уведомление {
        +Текст : text
        +Прочитано : bool
        +прочитать()
        +отправить_в_телеграм()
    }

    class СтатьяБюджета {
        +Категория : BudgetCategory
        +Сумма_план : float
        +Сумма_факт : float
        +обновить_затраты(сумма)
    }

    class ИсторияЗадачи {
        +Дата_изменения : datetime
        +сохранить_состояние()
    }

    class СистемныйЛог {
        +Действие : text
        +регистрация_события()
    }

    %% Справочные классы (без методов)
    class Роль { +Имя : string }
    class СтатусПроекта { +Имя : string }
    class СтатусЗадачи { +Имя : string }
    class ТипДокумента { +Имя : string }
    class КатегорияБюджета { +Имя : string }

    %% Связи
    Пользователь "1" --> "0..*" Проект : "участвует"
    Пользователь "1" --> "0..*" Задача : "исполняет"
    Пользователь "1" --> "0..*" Уведомление : "получает"
    Проект "1" *-- "0..*" Задача : "содержит"
    Проект "1" *-- "0..*" ЭтапПроекта : "разбит на"
    Проект "1" *-- "0..*" Документ : "имеет"
    Проект "1" *-- "0..*" СтатьяБюджета : "бюджетируется"
    Задача "1" *-- "0..*" Комментарий : "обсуждается"
    Задача "1" *-- "0..*" ЛогРаботы : "учет времени"
    Задача "1" *-- "0..*" ИсторияЗадачи : "логируется"
    Роль "1" -- "0..*" Пользователь : "связана"
    СтатусПроекта "1" -- "0..*" Проект : "определяет"
    СтатусЗадачи "1" -- "0..*" Задача : "определяет"
